version: "3.9"

services:
  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    depends_on:
      - redis
    environment:
      SPRING_PROFILES_ACTIVE: test
      SPRING_REDIS_HOST: redis
      SPRING_REDIS_PORT: 6379

  redis:
    image: redis:7
    ports:
      - "6379:6379"
